# Overview

This repository is a collection of Ubuntu 18.04 dotfiles for my development environment setup

It includes

- `vim` config
- `i3` config
- `polybar` config
- `compton` config
- `zsh` config
- `scripts` directory
